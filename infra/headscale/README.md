# Headscale (planned)

Tailscale's free tier caps you at 3 users. Once we outgrow that, Headscale is the
self-hosted control plane that lifts the cap for $0. It runs on the Oracle VM.

Not built yet. Tracked as a future feature.
