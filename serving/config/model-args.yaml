# Model launch args for vLLM. Set these before running docker compose up.
# Example:
#   MODEL_NAME=Qwen/Qwen3-Coder-32B-Instruct
#   TENSOR_PARALLEL_SIZE=2
#   GPU_MEMORY_UTILIZATION=0.90
#   QUANTIZATION=awq

MODEL_NAME=
TENSOR_PARALLEL_SIZE=
GPU_MEMORY_UTILIZATION=
QUANTIZATION=